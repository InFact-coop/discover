import { createAction } from "redux-actions"
import { TOGGLE_SELECT_TECHNIQUE } from "../types"

// eslint-disable-next-line
export const toggleSelectTechnique = createAction(TOGGLE_SELECT_TECHNIQUE)
