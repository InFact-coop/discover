const initState = {
  titleColor: "#ffffff",
}
export default (state = initState, action) => {
  switch (action.type) {
    case "AAAAA": {
      return state
    }
    default:
      return state
  }
}
